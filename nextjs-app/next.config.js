/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/encode_heart',
  assetPrefix: '/encode_heart',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
